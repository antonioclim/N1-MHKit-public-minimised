# Browser-only use note

This public package is designed to be deposited as a single ZIP file on Zenodo and as a GitHub release asset. Users who do not run command-line tools can inspect README, metadata tables, reports and processed public-minimised CSV files directly after extraction.
