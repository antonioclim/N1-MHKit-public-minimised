# Technical architecture - public-minimised release

The public package documents a schema-first Design Science Research artifact. The public layer contains processed public-minimised tables and metadata. The restricted verification layer, not included here, preserves the full source-to-output reproduction path for editor-side verification if necessary.
