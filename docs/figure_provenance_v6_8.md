# Figure provenance note (v6.8 final clean)

Figure 1 and Figure 2 are model-driven schematic diagrams generated by deterministic Python scripts from CSV figure-source files in `figure_sources/`. Supplementary Figure S1 is generated from public-minimised paired values in `data_processed_public/clean_measurements_wide_public_minimised.csv`.

No generative AI image model was used to create, generate or alter any figure or artwork.

Regenerate all figures with:

```bash
python code/figures/generate_all_manuscript_figures.py
```

Generated figure files are supplied as PDF, SVG and high-resolution PNG exports.
